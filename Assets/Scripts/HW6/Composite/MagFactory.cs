using System.IO;
using UnityEngine;

namespace hw6
{
    public class MagFactory : IMagFactory
    {
        public Mag Create(string path)
        {
            //Парсит JSON И возвращает объект Mag
            return null;
        }
    }
}